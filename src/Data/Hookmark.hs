module Data.Hookmark where

import System.IO
import Data.List

-- dispatch :: String -> [String] -> IO ()
-- dispatch "-v" = version
-- dispatch "-help" = help
-- dispatch "add" = add
-- dispatch "rm" = remove
-- dispatch "ls" = ls
-- dispatch "pin" = pin
-- dispatch _ = error


